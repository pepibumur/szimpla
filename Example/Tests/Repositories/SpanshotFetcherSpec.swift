import Foundation
import Quick
import Nimble

@testable import Szimpla

class SpanshotFetcher: QuickSpec {
    
    override func spec() {
        describe("-fetch") {
            context("when the snapshot exists") {
                
            }
            
            context("when the snapshot doesn't exist") {
                
            }
        }
    }
    
}

